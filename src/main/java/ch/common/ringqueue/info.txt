

// 环形队列的使用

1.创建一个包含3600个solt（卡槽）的环形队列（本质就是一个数组）

2.任务集合，每个卡槽上面是一个set<Task>

3.启动Timer,它每隔一秒在上述环上移动一格，有一个 current Index 指针来标识正在校验的卡槽，
  环形分为3600个长度，正好一个小时，比如一个任务延迟60秒，当前指针指向卡槽2，那么这个数据应该放到
  62的卡槽上面，如果时间超过3600，就用圈数来表示。
